import 'package:flutter/material.dart';

class HomaView extends StatelessWidget {
  const HomaView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: const Text("Hey"),
    );
  }
}